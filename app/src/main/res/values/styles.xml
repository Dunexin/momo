<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="HomeActivityTheme"
        parent="@style/Theme.AppCompat.Light">

        <item name="android:actionBarStyle">@style/BaseTheme</item>
        <item name="android:logo">@drawable/gco</item>
        <item name="logo">@drawable/gco</item>
    </style>

    <style name="BaseTheme"
        parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:background">@drawable/hpr</item>

        <item name="background">@drawable/hpr</item>
    </style>

</resources>
